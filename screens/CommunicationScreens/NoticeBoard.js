import * as React from "react";
import { View, SafeAreaView, Text, TouchableOpacity } from "react-native";

const NoticeBoard= () => {
  return <Text>NoticeBoard</Text>;
};

export default NoticeBoard;
